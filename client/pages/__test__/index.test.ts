// import test from "node:test";
import index, { add } from "../components/index";
import { test, expect, it } from "vitest";

test("", () => {
  it("", () => {
    expect(add(1, 2)).toEqual(3);
  });
});
